[project]
name = "dspy-toon"
version = "0.4.0"
description = "DSPy adapter using TOON (Token-Oriented Object Notation) v4.1 format for compact structured LLM inputs and outputs"
readme = "README.md"
authors = [
    { name = "Michael Pavlukhin", email = "michael@pavlukhinlab.com" }
]
requires-python = ">=3.10,<3.15"
dependencies = [
    "dspy>=3.3.1,<4",
    "pydantic>=2.0.0",
]
license = "MIT"
license-files = ["LICENSE", "NOTICE"]
keywords = [
    "dspy",
    "toon",
    "toon-format",
    "llm",
    "adapter",
    "structured-output",
    "token-efficient",
    "token-reduction",
    "serialization",
    "serialization-format",
    "ai",
    "machine-learning",
    "nlp",
    "prompt-engineering",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/Archelunch/dspy-toon"
Repository = "https://github.com/Archelunch/dspy-toon"
Documentation = "https://github.com/Archelunch/dspy-toon#readme"
"Bug Tracker" = "https://github.com/Archelunch/dspy-toon/issues"

[project.optional-dependencies]
benchmark = [
    "tiktoken>=0.4.0",
    "pandas>=2.0.0",
    "tabulate>=0.9.0",
    "datasets>=4.4.1",
    "httpx>=0.28.1",
    "jsonschema>=4.0.0",
    "pyarrow>=21.0.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.8.0",
    "mypy>=1.8.0",
]
all = [
    "dspy-toon[benchmark,dev]",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "-ra",
]
asyncio_mode = "auto"

[tool.coverage.run]
relative_files = true
source = ["src"]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
]
ignore = ["N"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.10"
warn_return_any = false
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = false

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
core-metadata-version = "2.4"
packages = ["src/dspy_toon"]

[tool.hatch.build.targets.sdist]
core-metadata-version = "2.4"
only-include = [
    "src/dspy_toon",
    "pyproject.toml",
    "README.md",
    "LICENSE",
    "NOTICE",
]
